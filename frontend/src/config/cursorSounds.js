import defaultClick from '../assets/sounds/clickSFX.mp3';

const soundEffects = {
  click_default: defaultClick,
  click_primary: defaultClick, // Replace with different sound files
  click_secondary: defaultClick,
  hover_default: defaultClick,
  hover_primary: defaultClick,
  hover_secondary: defaultClick,
};

export default soundEffects;
